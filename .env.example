# Base URL for backend API calls (SSR + client fetch wrapper)
API_BASE_URL=https://api.example.com

# Same value can be re-used on the client if needed
NEXT_PUBLIC_API_BASE_URL=https://api.example.com

# Session cookie name inspected by middleware/session util
SESSION_COOKIE_NAME=reetry.session
